{
  "name": "play_scala",
  "version": "1.0.0",
  "scripts": {
    "build": "sbt dist && set -x && unzip -d svc ./target/universal/*-1.0.zip && mv svc/*/* svc/ && rm svc/bin/*.bat && mv svc/bin/* svc/bin/start && docker build -t play_scala . && rm -rf svc"
  },
  "dependencies": {
    
  }
}
